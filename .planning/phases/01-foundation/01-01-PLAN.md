---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/web/package.json
  - dashboard/web/next.config.ts
  - dashboard/web/tsconfig.json
  - dashboard/web/postcss.config.mjs
  - dashboard/web/tailwind.config.ts
  - dashboard/web/app/globals.css
  - dashboard/web/app/layout.tsx
  - dashboard/web/app/page.tsx
  - dashboard/web/.gitignore
  - dashboard/web/public/.gitkeep
  - dashboard/web/README.md
autonomous: false
requirements: [FND-01, FND-02]
must_haves:
  truths:
    - "From dashboard/web/, npm install completes without errors"
    - "From dashboard/web/, npm run dev serves http://localhost:3000 returning HTTP 200"
    - "Page at http://localhost:3000 visibly renders the word 'Today.' in Bricolage Grotesque (display font), on cream #faf6ee background, in deep ink #0a0a08"
    - "Tailwind v4 utility classes 'bg-paper', 'text-ink', 'border-ink', 'accent-cyber', 'accent-magenta', 'accent-acid', 'font-display', 'font-body', 'font-mono' resolve to DESIGN.md token values (verifiable via inspecting computed styles)"
    - "Y2K signature surface (2.5px solid ink border + 6px offset box-shadow) is demonstrated on at least one element of the smoke page"
    - "From repo root, `node update-system.mjs check` continues to exit 0 (santifer system layer untouched)"
  artifacts:
    - path: "dashboard/web/package.json"
      provides: "Next.js 15 + React 19 + TypeScript dependencies + npm scripts (dev, build, start, lint)"
      contains: "next"
    - path: "dashboard/web/tsconfig.json"
      provides: "TypeScript strict mode configuration"
      contains: "\"strict\": true"
    - path: "dashboard/web/app/globals.css"
      provides: "Tailwind v4 @theme block exposing all DESIGN.md tokens as CSS variables"
      contains: "@theme"
    - path: "dashboard/web/app/layout.tsx"
      provides: "Root layout with Bricolage Grotesque + General Sans + IBM Plex Mono font links and body classes"
      contains: "Bricolage+Grotesque"
    - path: "dashboard/web/app/page.tsx"
      provides: "Smoke page rendering 'Today.' hero in display font, demonstrating Y2K card signature"
      contains: "Today."
    - path: "dashboard/web/tailwind.config.ts"
      provides: "Tailwind v4 config pointing content to ./app and ./components (v4 is CSS-first; this file is minimal but exists for token discoverability)"
      contains: "content"
  key_links:
    - from: "dashboard/web/app/layout.tsx"
      to: "dashboard/web/app/globals.css"
      via: "import './globals.css'"
      pattern: "import.*globals\\.css"
    - from: "dashboard/web/app/globals.css"
      to: "Tailwind v4 @theme tokens"
      via: "@theme block defining --color-bg, --color-cyber, --color-magenta, --color-acid, --font-display"
      pattern: "@theme"
    - from: "dashboard/web/app/layout.tsx"
      to: "Google Fonts + Fontshare CDN"
      via: "<link rel=\"stylesheet\" href=\"https://fonts.googleapis.com/css2?family=Bricolage+Grotesque...\">"
      pattern: "fonts\\.googleapis\\.com"
---

<objective>
Stand up the Next.js 15 application skeleton at `dashboard/web/` with TypeScript strict mode, Tailwind v4 CSS-first config wiring every DESIGN.md token, and a smoke page proving the Y2K aesthetic renders end-to-end. Confirm santifer system layer remains untouched (`npm run update:check` still passes from repo root).

Purpose: Provides the foundation that Phases 2-5 build on. Without this plan, no other plan in the project can run. Locks DESIGN.md tokens into Tailwind so all subsequent component work consumes tokens (not raw hex literals).

Output: A `dashboard/web/` directory with installable Next.js app, running dev server, and visible DESIGN.md typography. Satisfies FND-01 and FND-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@DESIGN.md
@DATA_CONTRACT.md
@AGENTS.md
@CLAUDE.md
@package.json

<interfaces>
<!-- DESIGN.md tokens that MUST be wired verbatim into Tailwind v4 @theme block. -->
<!-- Source of truth: DESIGN.md sections "Typography", "Color", "Spacing", "Layout". -->

Colors (CSS custom properties in @theme):
  --color-bg:         #faf6ee   /* warm cream paper */
  --color-paper:      #fff9ec   /* card surface */
  --color-ink:        #0a0a08   /* deep ink */
  --color-ink-soft:   #2a2a26
  --color-ink-muted:  #5a5a52
  --color-ink-dim:    #8a8a82
  --color-chrome:     #d4d4dc

  --color-cyber:      #00d4ff   /* interactive / action */
  --color-magenta:    #ff006e   /* attention / overdue / error */
  --color-acid:       #b8ff00   /* success / applied / progress */

  --color-cyber-soft:   #00d4ff20
  --color-magenta-soft: #ff006e15
  --color-acid-soft:    #b8ff0030

Fonts (Tailwind v4 --font-* tokens):
  --font-display: "Bricolage Grotesque", system-ui, sans-serif
  --font-body:    "General Sans", system-ui, sans-serif
  --font-mono:    "IBM Plex Mono", ui-monospace, monospace

Type scale (--text-* tokens, 8pt base ratio 1.333):
  --text-xs:   0.625rem  /* 10px */
  --text-sm:   0.75rem   /* 12px */
  --text-base: 0.875rem  /* 14px */
  --text-md:   1rem      /* 16px */
  --text-lg:   1.125rem  /* 18px */
  --text-xl:   1.375rem  /* 22px */
  --text-2xl:  1.75rem   /* 28px */
  --text-3xl:  2.25rem   /* 36px */
  --text-4xl:  3rem      /* 48px */
  --text-5xl:  4.5rem    /* 72px */

Spacing (--spacing-* tokens, 4px base):
  --spacing-2xs: 0.125rem  /* 2px */
  --spacing-xs:  0.25rem   /* 4px */
  --spacing-sm:  0.5rem    /* 8px */
  --spacing-md:  1rem      /* 16px */
  --spacing-lg:  1.5rem    /* 24px */
  --spacing-xl:  2rem      /* 32px */
  --spacing-2xl: 3rem      /* 48px */
  --spacing-3xl: 4rem      /* 64px */

Border radius:
  --radius-none: 0       /* default for cards, buttons */
  --radius-sm:   4px     /* nav items */
  --radius-md:   8px
  --radius-lg:   14px    /* progress card only */

Font CDN links (paste in <head> of layout.tsx):
  <link href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,400;12..96,600;12..96,800&family=IBM+Plex+Mono:wght@400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://api.fontshare.com/v2/css?f[]=general-sans@400,500,600,700&display=swap">
</interfaces>

<forbidden_files>
Touching ANY of these files in this plan is a hard violation of DATA_CONTRACT.md:
- update-system.mjs (santifer system)
- templates/* (santifer system)
- modes/* (santifer system, except modes/_profile.md which is user layer but out of scope here)
- AGENTS.md (santifer system)
- dashboard/main.go, dashboard/internal/**, dashboard/index.html (existing Go TUI — Phase 1 leaves it alone)
- data/*, reports/*, output/* (user data)
- cv.md, config/profile.yml, portals.yml (user-personalized)
- package.json AT REPO ROOT (santifer-managed; new dashboard work lives in dashboard/web/package.json which is a separate file)
</forbidden_files>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 + TypeScript + Tailwind v4 dependency tree and tsconfig</name>
  <files>dashboard/web/package.json, dashboard/web/next.config.ts, dashboard/web/tsconfig.json, dashboard/web/.gitignore, dashboard/web/public/.gitkeep, dashboard/web/README.md</files>
  <read_first>
    - DESIGN.md (full file — tokens consumed in Task 2)
    - DATA_CONTRACT.md (verify dashboard/web/ scope is allowed and dashboard/* listed under system but DASHBOARD/WEB is new user-layer subtree per PROJECT.md constraint)
    - AGENTS.md (santifer constraints, ethical use, headless mode notes)
    - package.json (repo root — confirm scripts you must NOT replace; this is a DIFFERENT file from dashboard/web/package.json)
    - .planning/PROJECT.md (Key Decisions table — Next.js 15 + React 19 + Tailwind v4 + Framer Motion v12 locked)
  </read_first>
  <action>
    Create the following files with EXACT content shapes specified.

    **1. `dashboard/web/package.json`** — Use this exact JSON:
    ```json
    {
      "name": "career-ops-dashboard-web",
      "version": "0.1.0",
      "private": true,
      "scripts": {
        "dev": "next dev",
        "build": "next build",
        "start": "next start",
        "lint": "next lint"
      },
      "dependencies": {
        "next": "^15.0.0",
        "react": "^19.0.0",
        "react-dom": "^19.0.0"
      },
      "devDependencies": {
        "@types/node": "^22.0.0",
        "@types/react": "^19.0.0",
        "@types/react-dom": "^19.0.0",
        "typescript": "^5.6.0",
        "tailwindcss": "^4.0.0",
        "@tailwindcss/postcss": "^4.0.0",
        "postcss": "^8.4.0"
      }
    }
    ```

    **2. `dashboard/web/next.config.ts`** — Minimal Next.js 15 config:
    ```ts
    import type { NextConfig } from "next";

    const nextConfig: NextConfig = {
      reactStrictMode: true,
    };

    export default nextConfig;
    ```

    **3. `dashboard/web/tsconfig.json`** — Strict mode, Next.js 15 defaults:
    ```json
    {
      "compilerOptions": {
        "target": "ES2022",
        "lib": ["dom", "dom.iterable", "esnext"],
        "allowJs": false,
        "skipLibCheck": true,
        "strict": true,
        "noEmit": true,
        "esModuleInterop": true,
        "module": "esnext",
        "moduleResolution": "bundler",
        "resolveJsonModule": true,
        "isolatedModules": true,
        "jsx": "preserve",
        "incremental": true,
        "plugins": [{ "name": "next" }],
        "paths": { "@/*": ["./*"] }
      },
      "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
      "exclude": ["node_modules"]
    }
    ```

    **4. `dashboard/web/.gitignore`** — Next.js standard ignores:
    ```
    # Dependencies
    node_modules/

    # Next.js
    .next/
    out/

    # Production
    build/

    # Misc
    .DS_Store
    *.pem

    # Debug
    npm-debug.log*
    yarn-debug.log*
    yarn-error.log*

    # Local env
    .env*.local

    # Vercel
    .vercel

    # TypeScript
    *.tsbuildinfo
    next-env.d.ts

    # Test artifacts
    /test-results/
    /playwright-report/
    /playwright/.cache/
    coverage/
    ```

    **5. `dashboard/web/public/.gitkeep`** — Empty file (placeholder so directory ships).

    **6. `dashboard/web/README.md`** — Brief readme:
    ```markdown
    # career-ops Dashboard — Web (Next.js)

    Local web dashboard for the career-ops AI job-search pipeline.

    See `/.planning/` at repo root for full project context, requirements, and phase plans.
    See `/DESIGN.md` for the Y2K maximalist design system.

    ## Local dev

    ```bash
    npm install
    npm run dev
    ```

    Then open http://localhost:3000.

    ## Layout

    - `app/` — Next.js 15 App Router pages and layouts
    - `components/` — Motion-wrapped components (Phase 4)
    - `components/raw/` — Magic MCP base components (Phase 4)
    - `lib/` — Parsers, schemas, spawn, lock helpers (Phase 2)
    - `tests/` — Vitest unit tests
    - `e2e/` — Playwright end-to-end tests

    ## Constraints

    - Scoped strictly to `dashboard/web/` per `/DATA_CONTRACT.md`.
    - santifer system layer (`update-system.mjs`, `modes/`, `templates/`) must remain untouched.
    ```

    After creating files, run `cd dashboard/web && npm install` to install all dependencies. This will generate `package-lock.json` and `node_modules/`. Both are normal artifacts — `node_modules/` is gitignored; commit `package-lock.json`.

    Do NOT touch the repo root `package.json` — that file is santifer-managed. `dashboard/web/package.json` is a new, separate file.
  </action>
  <acceptance_criteria>
    - `dashboard/web/package.json` exists and contains the string `"next": "^15.0.0"` (grep verifiable)
    - `dashboard/web/package.json` contains `"react": "^19.0.0"` and `"react-dom": "^19.0.0"`
    - `dashboard/web/package.json` contains `"tailwindcss": "^4.0.0"` in devDependencies
    - `dashboard/web/tsconfig.json` contains `"strict": true`
    - `dashboard/web/tsconfig.json` contains `"jsx": "preserve"` and `"moduleResolution": "bundler"`
    - `dashboard/web/next.config.ts` exists and `import type { NextConfig } from "next"` is present
    - `dashboard/web/.gitignore` contains both `node_modules/` and `.next/`
    - `dashboard/web/public/.gitkeep` exists (empty file is fine)
    - `dashboard/web/README.md` exists and references `/DESIGN.md` and `/.planning/`
    - `cd dashboard/web && npm install` exits 0 and creates `dashboard/web/package-lock.json` and `dashboard/web/node_modules/`
    - Repo root `package.json` is byte-identical to its state before this task ran (verify with `git diff package.json` showing no changes)
    - `git status` shows no modifications to `update-system.mjs`, `templates/*`, `modes/*`, `AGENTS.md`, `dashboard/main.go`, `dashboard/index.html`, or `dashboard/internal/**`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/michalmaciejewski/Desktop/jobs/career-ops/dashboard/web && npm install --no-audit --no-fund 2>&1 | tail -5 && test -f package-lock.json && test -d node_modules && grep -q '"strict": true' tsconfig.json && grep -q '"next": "\^15' package.json && echo "OK"</automated>
  </verify>
  <done>
    Next.js 15 + React 19 + TypeScript strict + Tailwind v4 dependency tree installed cleanly in `dashboard/web/`. `package-lock.json` committed; `node_modules/` ignored. Santifer system layer files have zero modifications.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire DESIGN.md tokens into Tailwind v4 (@theme), Next.js app layout, and Y2K smoke page</name>
  <files>dashboard/web/postcss.config.mjs, dashboard/web/tailwind.config.ts, dashboard/web/app/globals.css, dashboard/web/app/layout.tsx, dashboard/web/app/page.tsx</files>
  <read_first>
    - DESIGN.md (full file — every token defined here must appear in globals.css @theme block)
    - dashboard/web/package.json (created in Task 1 — confirms Tailwind v4 + @tailwindcss/postcss are installed)
    - dashboard/web/tsconfig.json (created in Task 1 — confirms strict TS and @/* path alias)
    - CLAUDE.md (Design System section pointing to DESIGN.md as source of truth)
  </read_first>
  <action>
    Tailwind v4 is CSS-first: tokens live in `app/globals.css` under `@theme { ... }` and are auto-exposed as utility classes (e.g. `--color-cyber` becomes `bg-cyber`, `text-cyber`, `border-cyber`). A `tailwind.config.ts` is kept for IDE discoverability but is intentionally minimal.

    **1. `dashboard/web/postcss.config.mjs`** — Tailwind v4 PostCSS plugin:
    ```js
    const config = {
      plugins: {
        "@tailwindcss/postcss": {},
      },
    };

    export default config;
    ```

    **2. `dashboard/web/tailwind.config.ts`** — Minimal v4 config (tokens are in CSS):
    ```ts
    import type { Config } from "tailwindcss";

    const config: Config = {
      content: [
        "./app/**/*.{ts,tsx}",
        "./components/**/*.{ts,tsx}",
      ],
    };

    export default config;
    ```

    **3. `dashboard/web/app/globals.css`** — Tailwind v4 @theme block with EVERY DESIGN.md token. Use this exact content:
    ```css
    @import "tailwindcss";

    @theme {
      /* === Colors === */
      --color-bg: #faf6ee;
      --color-paper: #fff9ec;
      --color-ink: #0a0a08;
      --color-ink-soft: #2a2a26;
      --color-ink-muted: #5a5a52;
      --color-ink-dim: #8a8a82;
      --color-chrome: #d4d4dc;

      --color-cyber: #00d4ff;
      --color-magenta: #ff006e;
      --color-acid: #b8ff00;

      --color-cyber-soft: #00d4ff20;
      --color-magenta-soft: #ff006e15;
      --color-acid-soft: #b8ff0030;

      /* === Fonts === */
      --font-display: "Bricolage Grotesque", system-ui, sans-serif;
      --font-body: "General Sans", system-ui, sans-serif;
      --font-mono: "IBM Plex Mono", ui-monospace, monospace;

      /* === Type scale (8pt base, ratio 1.333) === */
      --text-xs: 0.625rem;
      --text-sm: 0.75rem;
      --text-base: 0.875rem;
      --text-md: 1rem;
      --text-lg: 1.125rem;
      --text-xl: 1.375rem;
      --text-2xl: 1.75rem;
      --text-3xl: 2.25rem;
      --text-4xl: 3rem;
      --text-5xl: 4.5rem;

      /* === Spacing (4px base) === */
      --spacing-2xs: 0.125rem;
      --spacing-xs: 0.25rem;
      --spacing-sm: 0.5rem;
      --spacing-md: 1rem;
      --spacing-lg: 1.5rem;
      --spacing-xl: 2rem;
      --spacing-2xl: 3rem;
      --spacing-3xl: 4rem;

      /* === Border radius (chunky-flat) === */
      --radius-none: 0;
      --radius-sm: 4px;
      --radius-md: 8px;
      --radius-lg: 14px;
    }

    /* === Base body === */
    body {
      background: var(--color-bg);
      color: var(--color-ink);
      font-family: var(--font-body);
      font-size: var(--text-md);
      line-height: 1.5;
      -webkit-font-smoothing: antialiased;
    }
    ```

    **4. `dashboard/web/app/layout.tsx`** — Root layout with DESIGN.md font CDN links:
    ```tsx
    import type { Metadata } from "next";
    import "./globals.css";

    export const metadata: Metadata = {
      title: "career-ops",
      description: "Local dashboard for the career-ops AI job-search pipeline",
    };

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode;
    }) {
      return (
        <html lang="en">
          <head>
            <link
              href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,400;12..96,600;12..96,800&family=IBM+Plex+Mono:wght@400;500;600&display=swap"
              rel="stylesheet"
            />
            <link
              rel="stylesheet"
              href="https://api.fontshare.com/v2/css?f[]=general-sans@400,500,600,700&display=swap"
            />
          </head>
          <body>{children}</body>
        </html>
      );
    }
    ```

    **5. `dashboard/web/app/page.tsx`** — Smoke page proving tokens render. Visible hero "Today." in Bricolage Grotesque + Y2K card with 2.5px ink border + 6px offset ink shadow. Use INLINE styles for the Y2K signature (Tailwind v4 lacks first-class arbitrary-offset shadow utility shorthands for this, and the goal is to prove tokens flow end-to-end).
    ```tsx
    export default function Page() {
      return (
        <main className="bg-bg text-ink min-h-screen p-3xl">
          <p
            className="text-mono text-xs text-ink-muted mb-md"
            style={{ fontFamily: "var(--font-mono)" }}
          >
            // smoke test — phase 1
          </p>

          <h1
            className="font-display text-5xl"
            style={{
              fontFamily: "var(--font-display)",
              fontVariationSettings: '"wdth" 60',
              fontWeight: 800,
            }}
          >
            Today.
          </h1>

          <div
            className="mt-2xl p-lg bg-paper"
            style={{
              border: "2.5px solid var(--color-ink)",
              boxShadow: "6px 6px 0 var(--color-ink)",
              borderRadius: "var(--radius-none)",
              maxWidth: "480px",
            }}
          >
            <p
              className="text-sm text-mono mb-sm"
              style={{ fontFamily: "var(--font-mono)" }}
            >
              // y2k signature
            </p>
            <p className="text-md" style={{ fontFamily: "var(--font-body)" }}>
              2.5px ink border + 6px offset shadow + flat corners. Body type is
              General Sans.
            </p>

            <div className="mt-md flex gap-sm">
              <span
                className="px-sm py-2xs text-xs"
                style={{
                  background: "var(--color-cyber)",
                  color: "var(--color-ink)",
                  fontFamily: "var(--font-mono)",
                  fontWeight: 600,
                }}
              >
                CYBER
              </span>
              <span
                className="px-sm py-2xs text-xs"
                style={{
                  background: "var(--color-magenta)",
                  color: "var(--color-ink)",
                  fontFamily: "var(--font-mono)",
                  fontWeight: 600,
                }}
              >
                MAGENTA
              </span>
              <span
                className="px-sm py-2xs text-xs"
                style={{
                  background: "var(--color-acid)",
                  color: "var(--color-ink)",
                  fontFamily: "var(--font-mono)",
                  fontWeight: 600,
                }}
              >
                ACID
              </span>
            </div>
          </div>
        </main>
      );
    }
    ```

    Rationale on inline styles: Tailwind v4 maps `--color-*` to `bg-*`/`text-*`/`border-*` utilities automatically, BUT non-color tokens like `--font-display` and the Y2K signature (multi-property shadow + border combo) are clearer when read from CSS vars directly. This is a smoke page; Phase 4 components will refactor to clean utility-only consumption once magic MCP generates the wrappers.

    Verify after writing: from `dashboard/web/` run `npm run dev`, open `http://localhost:3000`, confirm:
    - Cream cream-paper background
    - "Today." in tall condensed display font (Bricolage Grotesque variable wdth 60, weight 800)
    - Card with ink border + 6px offset shadow, flat corners
    - Three pills showing cyber / magenta / acid neon backgrounds
  </action>
  <acceptance_criteria>
    - `dashboard/web/app/globals.css` contains `@import "tailwindcss";`
    - `dashboard/web/app/globals.css` contains `@theme {` block
    - `dashboard/web/app/globals.css` contains ALL nine color tokens verbatim: `--color-bg: #faf6ee`, `--color-paper: #fff9ec`, `--color-ink: #0a0a08`, `--color-cyber: #00d4ff`, `--color-magenta: #ff006e`, `--color-acid: #b8ff00`, `--color-chrome: #d4d4dc`, `--color-ink-soft: #2a2a26`, `--color-ink-muted: #5a5a52` (grep each)
    - `dashboard/web/app/globals.css` contains font tokens: `--font-display: "Bricolage Grotesque"`, `--font-body: "General Sans"`, `--font-mono: "IBM Plex Mono"`
    - `dashboard/web/app/globals.css` contains type scale tokens `--text-xs` through `--text-5xl` (10 entries, all matching DESIGN.md rem values)
    - `dashboard/web/app/globals.css` contains spacing tokens `--spacing-2xs` through `--spacing-3xl` (8 entries)
    - `dashboard/web/app/globals.css` contains radius tokens `--radius-none: 0`, `--radius-sm: 4px`, `--radius-md: 8px`, `--radius-lg: 14px`
    - `dashboard/web/app/layout.tsx` contains `import "./globals.css"`
    - `dashboard/web/app/layout.tsx` contains the Google Fonts link with `Bricolage+Grotesque` AND `IBM+Plex+Mono`
    - `dashboard/web/app/layout.tsx` contains the Fontshare link with `general-sans`
    - `dashboard/web/app/page.tsx` contains the string `Today.` (the visible hero text)
    - `dashboard/web/app/page.tsx` references `border: "2.5px solid var(--color-ink)"` and `boxShadow: "6px 6px 0 var(--color-ink)"` (Y2K signature)
    - `dashboard/web/app/page.tsx` uses all three accent tokens (`--color-cyber`, `--color-magenta`, `--color-acid`) at least once
    - `dashboard/web/postcss.config.mjs` registers `@tailwindcss/postcss`
    - `dashboard/web/tailwind.config.ts` `content` array includes `./app/**/*.{ts,tsx}`
    - `cd dashboard/web && npm run build` exits 0 (proves Tailwind v4 + Next.js 15 compile together without error)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/michalmaciejewski/Desktop/jobs/career-ops/dashboard/web && grep -q "#faf6ee" app/globals.css && grep -q "#00d4ff" app/globals.css && grep -q "#ff006e" app/globals.css && grep -q "#b8ff00" app/globals.css && grep -q "Bricolage Grotesque" app/globals.css && grep -q "Today\." app/page.tsx && grep -q "2.5px solid var(--color-ink)" app/page.tsx && npm run build 2>&1 | tail -10 && test -d .next && echo "OK"</automated>
  </verify>
  <done>
    Tailwind v4 @theme block exposes every DESIGN.md token. Layout loads Bricolage + General Sans + IBM Plex Mono via CDN. Smoke page renders "Today." hero in Bricolage display font on cream bg with Y2K signature card (2.5px ink border + 6px offset shadow) and three neon accent pills. `npm run build` succeeds.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual checkpoint — Y2K aesthetic renders in browser</name>
  <what-built>
    Next.js 15 dev server serving `dashboard/web/app/page.tsx`, which renders the DESIGN.md Y2K aesthetic with three accent colors, Bricolage Grotesque display font, General Sans body, IBM Plex Mono labels, and the signature 2.5px ink border + 6px offset shadow card.
  </what-built>
  <how-to-verify>
    1. From a NEW terminal at repo root, run: `cd dashboard/web && npm run dev`
    2. Wait for the line `▲ Next.js 15... - Local: http://localhost:3000` (~3 seconds)
    3. Open http://localhost:3000 in your browser
    4. Visual checklist (confirm each):
       - Background is warm cream (`#faf6ee`), not pure white
       - "Today." appears in a tall, condensed, bold display font (Bricolage Grotesque, variable width 60, weight 800)
       - The `// smoke test — phase 1` and `// y2k signature` labels render in IBM Plex Mono
       - The body sentence ("2.5px ink border...") renders in General Sans
       - A card has a 2.5px solid black border and a 6px offset solid-black shadow (no blur — chunky-flat)
       - Three pills sit at the bottom of the card: cyber blue (`#00d4ff`), magenta (`#ff006e`), acid green (`#b8ff00`), each with bold ink text
       - No visual lag, no console errors in DevTools
    5. Open DevTools → Inspect the `<body>` element → confirm computed style `background-color: rgb(250, 246, 238)` (the cream)
    6. Inspect the "Today." heading → confirm computed `font-family` includes `"Bricolage Grotesque"`
    7. Stop the dev server (Ctrl-C in the dev terminal)

    If ANY visual fails, describe specifically what looks wrong (wrong font, wrong color, no shadow, etc.).
  </how-to-verify>
  <resume-signal>Type "approved" if all visual checks pass. Otherwise describe the issue and Claude will fix.</resume-signal>
  <files>(checkpoint — no files modified)</files>
  <action>Human-verification gate. Claude pauses execution while user runs `cd dashboard/web && npm run dev` and visually inspects http://localhost:3000 per the steps in <how-to-verify> above. Claude resumes after the resume-signal is received.</action>
  <verify>Manual visual inspection per <how-to-verify> checklist. No automated verification available — that is the point of this checkpoint.</verify>
  <done>User has typed "approved" confirming the Y2K aesthetic renders correctly in the browser (cream bg + Bricolage Today. + Y2K card with 2.5px ink border + 6px offset shadow + three neon accent pills).</done>
</task>

<task type="auto">
  <name>Task 4: Confirm santifer system layer is untouched (npm run update:check still passes)</name>
  <files>(verification only — no file modifications)</files>
  <read_first>
    - DATA_CONTRACT.md (the "User Layer" vs "System Layer" rules being verified)
    - package.json (repo root — the santifer-managed file we MUST NOT have touched)
    - update-system.mjs (to understand what `check` does)
  </read_first>
  <action>
    This task is a pure verification gate. It runs santifer's own update checker and confirms no system-layer files were modified by Plan 01. No new files are created.

    Run from REPO ROOT (NOT dashboard/web/):

    1. `node update-system.mjs check` — Must exit 0. Acceptable JSON outputs: `{"status": "up-to-date"}`, `{"status": "offline"}`, `{"status": "no-remote-version"}`, `{"status": "dismissed"}`, or `{"status": "update-available", ...}`. Any of these = exit 0 = pass. A crash or non-zero exit = FAIL.

    2. `git status --short` — Verify no modifications to forbidden files. The output must NOT contain any of these as modified (`M`):
       - `update-system.mjs`
       - `templates/...`
       - `modes/_shared.md`, `modes/oferta.md`, `modes/scan.md`, `modes/pdf.md`, etc. (any file in `modes/` except `modes/_profile.md`)
       - `AGENTS.md`
       - `package.json` (repo root)
       - `dashboard/main.go`, `dashboard/index.html`, `dashboard/internal/...`, `dashboard/go.mod`, `dashboard/go.sum`, `dashboard/contacts-data.js`

       Untracked NEW files in `dashboard/web/` are expected and correct (this is the new user-layer subtree).

    3. Confirm `dashboard/web/` files are present:
       - `dashboard/web/package.json`
       - `dashboard/web/app/page.tsx`
       - `dashboard/web/app/layout.tsx`
       - `dashboard/web/app/globals.css`

    If all three checks pass, this plan is done. If any check fails, ROLL BACK the offending file: restore santifer system files from git (`git checkout -- <file>`) and re-verify.
  </action>
  <acceptance_criteria>
    - From repo root: `node update-system.mjs check` exits 0
    - `git diff --name-only update-system.mjs templates/ modes/ AGENTS.md package.json dashboard/main.go dashboard/index.html dashboard/internal/ dashboard/go.mod dashboard/go.sum dashboard/contacts-data.js` returns EMPTY (no santifer system file modified)
    - `git status --short` shows new files only under `dashboard/web/` and NO `M` entries for any santifer system file listed above
    - All four required files exist: `dashboard/web/package.json`, `dashboard/web/app/page.tsx`, `dashboard/web/app/layout.tsx`, `dashboard/web/app/globals.css`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/michalmaciejewski/Desktop/jobs/career-ops && node update-system.mjs check && DIFF=$(git diff --name-only update-system.mjs templates/ modes/_shared.md modes/oferta.md modes/scan.md modes/pdf.md AGENTS.md package.json dashboard/main.go dashboard/index.html dashboard/go.mod dashboard/go.sum dashboard/contacts-data.js 2>/dev/null) && [ -z "$DIFF" ] && test -f dashboard/web/package.json && test -f dashboard/web/app/page.tsx && test -f dashboard/web/app/layout.tsx && test -f dashboard/web/app/globals.css && echo "OK"</automated>
  </verify>
  <done>
    `npm run update:check` (santifer's own update probe) still passes. Zero santifer system-layer files were modified by this plan. The new `dashboard/web/` subtree is the only thing that changed on disk (plus untracked `node_modules/`, `.next/`, `package-lock.json`). FND-04 satisfied.
  </done>
</task>

</tasks>

<verification>
End-to-end verification for Plan 01:

1. `cd dashboard/web && npm install` exits 0
2. `cd dashboard/web && npm run build` exits 0 (proves Next.js 15 + Tailwind v4 + TS strict compile cleanly together)
3. `cd dashboard/web && npm run dev` serves http://localhost:3000 returning HTTP 200 within 5 seconds
4. Visual inspection (Task 3 checkpoint) — Y2K aesthetic renders correctly
5. `node update-system.mjs check` from repo root still exits 0 (santifer integrity preserved)
6. `git status` shows new files ONLY under `dashboard/web/`; no modifications to any santifer system file
</verification>

<success_criteria>
- FND-01 satisfied: Next.js 15 + React 19 + TypeScript strict scaffold runs locally in `dashboard/web/`
- FND-02 satisfied: Tailwind v4 `@theme` block exposes every DESIGN.md token; smoke page renders Bricolage + cream + triple-neon + Y2K signature
- Phase 1 ready for Plan 02 (test infrastructure)
- Zero santifer system-layer modifications
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` documenting:
- Final file list under `dashboard/web/`
- Confirmed package versions installed (`next`, `react`, `tailwindcss`)
- Any deviations from this plan
- Pointer to Plan 02 for test infrastructure
</output>
